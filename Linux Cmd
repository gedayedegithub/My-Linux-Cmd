一、快速执行之前已经执行过的命令
  1、重复执行上一条命令
    1）使用上方向键，然后回车；
    2）使用!!，然后回车；
    3）使用快捷键ctr+p,然后回车；
    4）输入 !-1 并回车执行
  2、重复执行特定的历史命令
    0）使用上下方向键找到要执行的历史命令，然后回车；
       # 如果距离要执行的命令已经使用很久，用上方向键调出来就比较费时，这时可以选择使用以下方法：
    1）使用history 命令，调出之前使用过的所用命令列表，使用!Number(Number为要执行的命令前的编号)然后回车；
    2）!string：执行历史命令列表中最近的以string字符开头的命令；
    3）使用快捷ctr+r，输入相关字符，调出想要的命令后回车；
    4）!-n执行命令历史列表中的倒数第n条命令，!-1代表上一条命令。
